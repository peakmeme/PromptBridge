# 开发日记 - PeakMeme (PromptBridge)

## 2026-04-10：大模型本地化与弹药装填

今天完成了项目核心 Embedding 大模型的本地化集成，确保应用在无网或国内网络环境下依然能够实现高效的文本向量化。

### 1. 技术决策
- **模型选型**：选用 `BGE-Small-ZH-V1.5`，该模型在中文语义向量化方面表现优异且体积轻量（ONNX 量化版仅 24MB）。
- **运行环境**：完全本地化运行，不依赖外部 API。
- **存储方案**：直接将模型权重文件纳入 Git 仓库，实现克隆即用的“物理装填”体验。

### 2. 核心操作
- **目录构建**：建立了标准的 Xenova 模型加载路径 `assets/models/Xenova/bge-small-zh-v1.5/`。
- **模型装填**：通过 `curl` 从国内镜像源下载了全套 6 个核心文件：
    - `config.json`, `tokenizer.json`, `tokenizer_config.json`, `special_tokens_map.json`, `vocab.txt`
    - `onnx/model_quantized.onnx`
- **Git 策略**：由于模型文件体积可控，已将其从 `.gitignore` 中移除并推送到 GitHub 仓库。

### 3. 当前状态
- ✅ 模型资产已就绪（24MB）。
- ✅ GitHub 仓库同步完成。
- 🚀 下一步：编写 `EmbeddingManager.ts` 实现本地向量推理逻辑。
